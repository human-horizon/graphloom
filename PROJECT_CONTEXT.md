# Контекст проекта

[2026-08-24] Ошибка: `git diff --check` запущен в каталоге без Git-репозитория и завершился справкой Git → Решение: для проверки whitespace использовать `zsh -n`, `pnpm typecheck`, сборку и тесты; не использовать Git-команды в этом каталоге.
[2026-08-24] Ошибка: `open -a /Applications/Graphloom.app` сразу после переустановки вернул macOS error -600 → Решение: завершать старый процесс, ждать 1 секунду и запускать новый экземпляр через `open -n`.
[2026-08-24] Ошибка: после изменения renderer старые HTML продолжили отображаться из incremental cache → Решение: добавить `renderer_version` в cache key; изменения renderer теперь автоматически помечают старые отчёты pending. Позже заменено на полную перерисовку из DSL — renderer version больше не нужен.
[2026-08-24] Ошибка: при перерисовке готовых отчётов UI мог запускать `rerenderReport` параллельно и потенциально перетирать состояние → Решение: `refresh` делает последовательный rerender ready-файлов и ловит ошибки через `console.warn`.
[2026-08-25] Ошибка: LLM генерировал file/function-диаграммы с лишними узлами (33 узла на 80 строк), включая стандартные библиотечные вызовы, логирование и тривиальные проверки → Решение: обновлены промпты `file_map.md` и `function_flow.md` с ограничением 8-12/6-10 узлов и явным запретом на stdlib-утилиты и логирование; поднят `PROMPT_VERSION` в `state.rs` до `file-map-v2`, чтобы старые кэшированные отчёты инвалидировались. Graphloom.app переустановлен.
[2026-08-25] Ошибка: LLM указывал неверные номера строк в source-ссылках (например, подсвечивалась пустая строка вместо вызова) → Решение: в `validate.rs` добавлен `snap_node_source`: для узлов с `symbol` берётся точная позиция из UCM; для call/action/error/async узлов ищется ближайший UCM-call в том же файле (±5 строк) и подставляются его реальные start/end; итоговый диапазон clamp'ится к длине файла.
[2026-08-25] Semantic scope DSL v1 интегрирован end-to-end для Go file-level: Go analyzer выдаёт `entities` с AST-диапазонами и parent/children; Rust `semantic.rs` строит дерево, разворачивает в nodes+edges и применяет LLM-лейблы; `pipeline::analyze_file` теперь использует semantic tree + LLM labels; Qwen получает структурированное дерево и только лейблит. Mock server и fixture `labels.json` обновлены. `cargo test`, `clippy -D warnings`, `pnpm typecheck/build` — зелёные. Graphloom.app переустановлен. Осталось: TS analyzer entities, true tree renderer, cross_refs.
[2026-08-26] Ошибка: LLM часто возвращал labels JSON в markdown-огородке или с обрезанием, из-за чего apply_labels падал и использовал humanized-лейблы → Решение: apply_labels вырезает JSON из тела ответа; для project labels увеличен max_tokens до 8192. Теперь project/file/function labels применяются корректно на abyss.
[2026-08-26] Ошибка: cross_refs в renderer открывали только файл, но не функцию-цель → Решение: Reports.tsx при получении graphloom:navigate с symbolId/file сначала открывает файл, затем вызывает analyzeFunction для целевого символа и показывает его Flow.
[2026-08-26] Ошибка: install-mac.sh не пересобирал sidecar-ы после изменений исходников, оставляя устаревший Go/TS analyzer → Решение: sidecar build теперь безусловный (go build + pnpm build) в install-mac.sh.
[2026-08-27] Ошибка: file/project-деревья в renderer отображались абсолютно пустой чёрной областью из-за JS-ошибки `Cannot read properties of undefined (reading 'x')` в `treeLayout` → Решение: `treeLayout` теперь сохраняет позиции в `metrics._pos` и возвращает тот же объект метрик для каждого узла; после рендера дерево горизонтально центрируется, чтобы корневой узел был виден. Все тесты, clippy, typecheck и Tauri build — зелёные; Graphloom.app переустановлен.
[2026-08-27] Ошибка: схемы были перегруженными и большие деревья неудобно рассматривать; обычный клик по ноде сразу выполнял переход → Решение: renderer получил компактный переносимый tree-layout с прямыми parent-child связями, viewport с zoom/pan (`wheel`, `Space + drag`, кнопки и горячие клавиши), а обычный клик открывает sidebar; переход выполняется только через `⌘ + click`. Добавлены smoke-проверка browser runtime и проверки шаблона; production bundle собран и установлен.
[2026-08-27] Ошибка: прямые parent→каждый child создавали веер связей вместо последовательного flow → Решение: в tree-layout связи снова строятся по порядку исходника: родитель→первый ребёнок, затем последовательные siblings; для вложенных scope порядок строится рекурсивно. Browser screenshot подтверждает отсутствие веерного parent-to-all.
[2026-08-27] Улучшение: горизонтальная раскладка всё ещё плохо читалась как flow, а Command-click к символу терял предыдущий отчёт → Решение: scope tree теперь строится одной вертикальной колонкой сверху вниз; Reports.tsx хранит snapshot текущего HTML/file/symbol и показывает кнопку `Назад`, возвращающуюся по стеку command-навигации.
[2026-08-28] Ошибка: после переноса репозитория из `~/Space/HumanHorizon/graphloom` в `~/Space/Projects/HumanHorizon/graphloom` Cargo/Tauri target сохранял старые абсолютные пути, из-за чего сборка падала и приложение не находило Go analyzer → Решение: очищен `src-tauri/target`, sidecar-ы пересобраны, production bundle пересобран и установлен из нового корня.
[2026-08-28] Ошибка: при ошибке компиляции анализируемого проекта `get_file_tree` зависел от UCM, поэтому UI показывал выбранный путь и `0 files`, скрывая настоящую ошибку analyzer → Решение: `get_file_tree` теперь сканирует поддерживаемые Go/TS/TSX-файлы напрямую через WalkDir, а `Reports.refresh` сохраняет найденные файлы и показывает ошибку анализа со статусом error.
[2026-08-28] Ошибка: `scripts/install-mac.sh` без TTY завершался на `pnpm install` с `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`, но из-за `--silent` причина была скрыта → Решение: установщик теперь выставляет `CI=true`, чтобы pnpm корректно работал в неинтерактивном режиме.
[2026-08-28] Ошибка: установленный из Finder Graphloom передавал Go analyzer урезанный `PATH`, и `go/packages` не находил команду `go` → Решение: analyzer.rs ищет Go в текущем PATH и через login shell, добавляет каталог найденного executable в PATH дочернего Go sidecar; при отсутствии Go возвращается диагностическая ошибка.
[2026-08-28] Улучшение: вертикальный tree-layout выглядел как плоская линия и не показывал вложенность → Решение: для каждого узла с детьми добавлены рекурсивные полупрозрачные scope-region области, горизонтальный отступ по глубине и учёт границ областей в native scroll SVG.
[2026-08-28] Проблема: AI labels не объясняли часть строк, потому что analyzer и prompts разрешали пропуски → Решение: Go/TS analyzers создают coverage units с code/source, pipeline обрабатывает детерминированные окна с маркерами СДЕЛАНО/ДАЛЬШЕ, strict validator требует полный набор русских label/summary и coverage_ids, а grouping и декомпозиция выполняются только после проверки.
[2026-08-28] Проблема: у части cgo AST-единиц Abyss `code` был пустым при чтении исходника по byte offsets → Решение: Go analyzer сначала получает текст через `go/format.Node`, затем использует чтение исходника как fallback; все 3823 coverage units Abyss теперь имеют code.
[2026-08-28] Улучшение: границы вложенных областей сливались по краям → Решение: добавлен TREE_REGION_NESTING_GAP и увеличена прозрачность рамки; родительская область теперь имеет дополнительный зазор вокруг дочерней.
[2026-08-29] Проблема: экран Reports оставлял большие пустые зоны и не использовал viewport → Решение: Reports переведён на flex column, toolbar/status/header и панели уплотнены, report-layout/report-frame растягиваются на остаток окна, onboarding также заполняет оставшуюся высоту; renderer bar уменьшен до 36px.
[2026-08-28] Ошибка: проект `~/Space/Projects/Starframe/abyss` не анализировался из-за duplicate declarations в CSS/JS/layout и устаревших вызовов API → Решение: дубли и несовместимые вызовы устранены, `go build ./...`, `go vet ./...` и компиляция тестов проходят; Graphloom создал UCM из 19 пакетов/464 символов и отчёты на 174 и 4 ноды.
[2026-08-28] Улучшение: зум/панорамирование в renderer были лишними и путали со скроллом → Решение: схема рисуется в естественных координатах, `<div id="stage">` использует `overflow: auto`; убраны viewport-кнопки, `transform`, обработчики wheel/pointer/keydown, `fitViewport`, `zoomAt`, `refit`, `panState`.
[2026-08-29] Проблема: tree renderer соединял все узлы глобальной цепочкой, а функции нельзя было свернуть → Решение: добавлены toggle `−/+` для group/function-узлов, фильтрация свёрнутых поддеревьев с подъёмом внешних edges к видимому заголовку, structural edges строятся по иерархии parent→child; соседние функции больше не соединяются цепочкой. Проверено headless smoke: 5→4 ноды и 4→3 edges после сворачивания функции; Graphloom переустановлен.
[2026-08-29] Проблема: file node ошибочно показывался как область и принимал связи → Решение: file-level tree теперь исключает file root из SVG и regions, отбрасывает edges к нему, а label и summary одновременно выводит в верхнем file-description renderer. Проверено headless screenshot/DOM: fileNode=false, fileEdge=false, header=true, функции остаются независимыми.
[2026-08-29] Ошибка: pnpm frontend checks запускались параллельно с installer и временно потеряли TypeScript lib.d.ts → Решение: installer и frontend checks запускать последовательно; исправление записано в /Users/a/Space/AI/mistakes/2026-08-29-graphloom-parallel-pnpm-install.md.
[2026-08-29] Проблема: один compile error в Go package останавливал analyzer и помечал весь проект ошибочным → Решение: Go/TS UCM теперь содержит per-file `errors`, валидные пакеты продолжают анализироваться, error-файл получает сохранённую error-схему с NodeKind::Error, а UI продолжает очередь остальных файлов.
[2026-08-29] Ошибка: первый partial-analysis E2E использовал stale Go sidecar и параллельно запускал mock server на занятом порту → Решение: sidecar пересобирать после изменений, error-only E2E не запускает mock LLM; ошибка записана в /Users/a/Space/AI/mistakes/2026-08-29-graphloom-stale-go-sidecar-and-parallel-e2e.md.
[2026-08-29] Ошибка: финальная Go-команда была запущена из корня без go.mod → Решение: Go build/test/vet выполнять из analyzers/go; ошибка записана в /Users/a/Space/AI/mistakes/2026-08-29-graphloom-final-go-command-path.md.
